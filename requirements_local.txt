-r requirements_api.txt

# Local (offline) ASR fallback — asr/whisper_local.py.
# Not needed by the production API path, which uses NVIDIA's hosted
# whisper-large-v3 endpoint (asr/whisper_nvidia.py) instead.
torch>=2.5
transformers>=4.46
accelerate>=1.0

# Silero VAD (vad.py) — used both by the CLI demo (test_main.py) and by
# api.py's /ws/barge-in endpoint. torchaudio is a transitive import of the
# cached snakers4/silero-vad hub module, not used directly.
torchaudio>=2.5
