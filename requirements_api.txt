fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
soundfile>=0.12
librosa>=0.10

# llm.py talks to Groq's OpenAI-compatible endpoint via this client.
openai>=1.50

# tts.py imports these unconditionally at module load (GTTSEngine playback
# and network fallback), so both are needed just to import api.py, not only
# for the local-CLI demo path.
pygame>=2.6
gTTS>=2.5

langchain>=1.3,<2.0
langchain-classic>=1.0.8,<2.0
langchain-ollama>=1.1.0,<2.0
langchain-chroma>=1.1.0,<2.0
langchain-text-splitters>=1.1.2,<2.0
chromadb>=1.3.5,<2.0
beautifulsoup4>=4.12
playwright>=1.48

# Slack live ingestion (slack_bot/listener.py) — Socket Mode needs aiohttp
slack_bolt>=1.20
aiohttp>=3.10
